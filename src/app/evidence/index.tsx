// Protected route: the evidence log. Guarded by the auth gate in _layout.tsx.
import { EvidenceLogScreen } from '@/screens/EvidenceLogScreen';

export default function EvidenceLog() {
  return <EvidenceLogScreen />;
}

// Terms and Privacy Policy reader. Available signed in or out, because someone
// deciding whether to create an account must be able to read them first.
import { LegalScreen } from '@/screens/LegalScreen';

export default function Legal() {
  return <LegalScreen />;
}
